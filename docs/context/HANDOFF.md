# Handoff — последняя сессия

> Обновляй в конце каждой сессии. AI читает первым.

## Сессия 2026-05-30 (post-release polish — главная + admin featured)

**Ветка:** `work-home-fix` → PR в `dev`

### Сделано

- [x] Mini App: подписи у всех карточек «Полезное»; у «Статей» — «советы и помощь» (одна строка)
- [x] Mini App: иконки — сердечко (статьи), человечек без стетоскопа (врачи)
- [x] Mini App: tap-feedback на карточках + микроанимации иконок при нажатии (`prefers-reduced-motion`)
- [x] Admin: счётчик «На главной: X / 3», колонка в таблице, toggle с лимитом в редакторе
- [x] Admin: адаптив списка статей — компактный ⭐ на мобилке, перенос тегов
- [x] Документация: context, roadmap, app/admin guides

### Перед деплоем

- [ ] PR в `dev` → merge → deploy app + admin

### Следующая сессия

- [ ] **Фаза 5 / PRD-03** — запись на приём (pending/confirm flow)
- [ ] Mobile app: M0 backend `/api/mobile/v1` (см. `docs/mobile/`)

### Заметки

- Выравнивание заголовков карточек при разной длине подписи — отложено (костыль: короткие подписи в одну строку)
- Featured сбрасывается при снятии с публикации (draft)
- Фаза 4 задеплоена; эта сессия — polish после релиза

---

## Шаблон

```markdown
## Сессия YYYY-MM-DD
**Цель:**
### Сделано
- [ ]
### Следующая сессия
- [ ]
```
