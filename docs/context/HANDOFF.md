# Handoff — последняя сессия

> Обновляй в конце каждой сессии. AI читает первым.

## Сессия 2026-06-01 (фаза 5 — C1, лимиты, UX, доки)

### Сделано в коде (ожидает push `dev` → deploy)

**Mini App (C1)**
- [x] Запись: услуга → дата → **время** (`time_slots`) → форма → «Мои заявки»
- [x] Отмена своей заявки (`PATCH …/booking/requests/{id}`)
- [x] Маска телефона, валидация формы, уведомления **сверху**, отступы в списке услуг
- [x] Правила услуги: возраст (опционально), предупреждение по возрасту

**Server**
- [x] `schedule_style` + миграция **016**; availability/time_slots для `time_slots`
- [x] Лимиты: по **кличке**, по **слоту**, настраиваемые `rules.limits` (на услугу/день)
- [x] Публичная отмена заявки клиентом
- [x] Бот: в уведомлениях **дата + время** при `slot_time`
- [x] BOOK-01 `d.full_name` (ранее)

**Admin**
- [x] Услуга: лимиты, возраст, тексты подтверждения/отклонения; без ручного «группа лимита» (операции → `cat_surgery` авто)
- [x] Расписание: при «Выбор времени» нет поля «Мест» — только окно «С»–«До»
- [x] Заявки: диалоги подтверждения/отклонения с текстом клиенту
- [x] CI lint React Hooks v7 (admin)

**Документация**
- [x] `передача портал запись` — context + phase-5 md/html + booking-for-clinic.html

### Деплой

Push `dev` → **Deploy server**, **Deploy app**, **Deploy admin** (по paths).

На VPS после server: миграция **016** (`schedule_style`).

Smoke:
1. Admin: УЗИ с «Выбор времени» — шаблон «С»–«До», календарь, заявки confirm/reject
2. Mini App: запись со временем, 2 кота (разные клички), отмена
3. Бот: время в сообщении чата и клиенту

### Следующая сессия

1. Prod smoke после деплоя C1
2. **ADM-02** — форма заявки в admin (backlog)
3. Очередь на освободившийся слот (backlog phase-5)

### Правило admin UI

Эталон: `BookingScreen`, `GroomingScreen` — `< sm`: карточки, `fullScreen` диалоги, scrollable tabs.

---

## Фаза 5

[phase-5-appointments.md](../phase-5-appointments.md) · [booking-for-clinic.html](../booking-for-clinic.html)
